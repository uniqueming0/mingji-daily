# 记账小程序 · 原型说明（会员页与统计页）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 原型说明-会员页与统计页 |
| 关联文档 | 《记账小程序产品需求文档.md》V1.2 |
| 覆盖页面 | 会员中心页 `pages/vip/vip`、统计页 `pages/stats/stats` |
| 版本范围 | V1.0 会员支付 + 统计；V1.1 AI 分析入口（统计页） |
| 文档用途 | UI 设计稿与前端实现的直接依据，文案可直接粘贴使用 |

> 约定：本文所有尺寸单位为微信小程序 `rpx`（设计稿基准 750 宽），括号内为对应 px（375 宽屏，1px = 2rpx）。

---

## 一、通用设计规范

### 1.1 色彩规范（记账类绿色系）

| 色板角色 | 变量名 | 色值 | 用途 |
| --- | --- | --- | --- |
| 品牌主色 | `--primary` | `#00B578` | 主按钮、选中态、金额「收入/结余为正」、进度条 |
| 主色按压 | `--primary-pressed` | `#009A66` | 主按钮按压态、链接按压 |
| 主色浅底 | `--primary-light` | `#E6F7F0` | 选中标签底色、权益高亮底、进度条背景 |
| 主色描边 | `--primary-border` | `#99E0C4` | 选中描边、次按钮描边 |
| 主色渐变 | `--primary-gradient` | `#00C784 → #00A86B` | 主按钮/状态卡可选的品牌渐变 |
| 支出红 | `--expense` | `#F5483B` | 支出金额、负结余、删除、错误、环比上涨 |
| 收入绿 | `--income` | `#00B578` | 收入金额（与主色一致） |
| 警告橙 | `--warning` | `#FF8A00` | 即将到期、额度接近上限、待处理提示 |
| 信息蓝 | `--info` | `#3B7CFF` | 链接、日期选择、可点击文本 |
| 会员金 | `--vip-gold` | `#C9A063` | 会员徽章、会员权益图标、到期高亮 |
| 强调金 | `--vip-badge` | `#F7B500` | 「推荐/早鸟/限时」角标、卖点星标 |
| 文字一级 | `--text-1` | `#1F2329` | 标题、金额主数字 |
| 文字二级 | `--text-2` | `#5C6370` | 正文、说明 |
| 文字三级 | `--text-3` | `#9AA0A8` | 次要信息、时间戳、占位 |
| 文字四级 | `--text-4` | `#C4C9CF` | 禁用态文字、占位符 |
| 分割线 | `--line` | `#ECEEF1` | 列表分割、卡片内分割 |
| 页面底色 | `--bg` | `#F5F6F8` | 页面全局背景 |
| 卡片底色 | `--card` | `#FFFFFF` | 卡片、弹窗、列表单元 |
| 遮罩 | `--mask` | `rgba(0,0,0,0.55)` | 弹窗遮罩 |

**配色原则**：金额方向遵循记账行业惯例——「支出 = 红 / 收入 = 绿 / 结余为正绿、为负红」；会员相关元素统一用「会员金 `#C9A063`」建立高级感，营销角标用「强调金 `#F7B500`」。

### 1.2 字体层级

| 层级 | rpx | px | 字重 | 行高 | 用途 |
| --- | --- | --- | --- | --- | --- |
| Display 特大金额 | 64 | 32 | 700 | 1.2 | 统计页总支出/结余主数字、会员价大字 |
| H1 页面标题 | 40 | 20 | 600 | 1.3 | 页面大标题、弹窗标题 |
| H2 区块标题 | 32 | 16 | 600 | 1.4 | 卡片标题、套餐档位名、权益项名 |
| Body 正文 | 28 | 14 | 400 | 1.5 | 正文、列表主文案、按钮文案 |
| Caption 辅助 | 24 | 12 | 400 | 1.5 | 说明、时间、单位、次要信息 |
| Tag 标签 | 20 | 10 | 500 | 1.4 | 角标、徽章、标签 |

- 金额数字统一使用等宽数字字体（如 `DIN Alternate` / `Bebas`，无则用系统字体加 `font-weight: 700` + `font-variant-numeric: tabular-nums`），避免跳动。
- 金额千分位分隔（`¥3,286.50`），人民币符号 ¥ 用 0.6 倍字号上标或同字号均可，保持全 App 一致。

### 1.3 卡片圆角与阴影

| 元素 | 圆角 | 阴影 |
| --- | --- | --- |
| 普通卡片 | 24rpx（12px） | `0 4rpx 20rpx rgba(31,35,41,0.06)` |
| 主按钮（胶囊） | 全圆角（高的一半） | `0 8rpx 20rpx rgba(0,181,120,0.28)` |
| 次按钮 | 48rpx（24px） | 无 |
| 弹窗/半屏面板 | 32rpx（16px） | `0 16rpx 48rpx rgba(0,0,0,0.12)` |
| 输入框/选择器 | 16rpx（8px） | 无 |
| 小标签/角标 | 8rpx（4px） | 无 |
| 媒体九宫格缩略图 | 12rpx（6px） | 无 |

### 1.4 按钮规范

| 类型 | 尺寸（高×左右内边距） | 样式 | 交互 |
| --- | --- | --- | --- |
| 主按钮 CTA | 96rpx × 32rpx，全宽或自适应 | 背景 `#00B578`（或渐变），文字 `#FFFFFF` 32rpx/500，胶囊圆角，带投影 | 按压态 `#009A66` + 缩放 0.98；点击触发主流程 |
| 次按钮 | 96rpx × 32rpx | 白底 + 2rpx `#00B578` 描边，文字 `#00B578` | 按压态 `#E6F7F0` 底；用于次要动作 |
| 文字按钮 | 自适应 | 无背景，文字 `#00B578`（或 `--info`），可带下划线 | 按压态变 `#009A66`；用于「重试/查看全部/我已支付」 |
| 幽灵按钮 | 96rpx × 32rpx | 半透明白底 `rgba(255,255,255,0.2)` + 白字 + 1rpx 白描边 | 用于深色底/图片上 |
| 禁用态 | 同上尺寸 | 背景 `#C4C9CF`（会员营销场景可用 `#B8E3D3`），文字 `#FFFFFF`，无投影 | 不可点击；必要时点击 Toast 说明原因（如「次数已用完」） |

**禁用判定**：支付按钮在「下单中/支付中」置为禁用并显示加载态；协议未勾选时「确认支付」置为禁用。

### 1.5 Toast 与弹窗规范

**Toast（轻提示）**

| 类型 | 样式 | 时长 | 示例文案 |
| --- | --- | --- | --- |
| 成功 | 深底 `rgba(17,17,17,0.85)`，白字 28rpx，圆角 16rpx，前置 ✓ 图标（主色） | 1.5s | 「开通成功」「已保存」「已取消支付」 |
| 失败 | 同底，前置 ✕ 图标（支出红） | 2s | 「支付失败，请重试」「网络异常」 |
| 加载 | 同底，前置 spinner | 至流程结束 | 「正在拉起支付…」「生成中…」 |
| 普通 | 同底，无图标 | 1.5s | 「当前为最新周期」「已复制」 |

- 位置居中偏下（距底部约 20% 屏高），最多两行，超长省略。
- 连续触发时后一条覆盖前一条，不做队列。

**弹窗（Modal / ActionSheet）**

| 类型 | 结构 | 规范 |
| --- | --- | --- |
| 居中 Modal | 白卡片宽约 560rpx，圆角 32rpx，标题 36rpx/600，正文 28rpx/`--text-2`，按钮横排（次左主右）或主按钮全宽 | 用于确认支付、二次确认、授权 |
| 底部 ActionSheet | 底部弹出，圆角 32rpx（仅上两角），顶部灰把手，项高 104rpx | 用于档位选择、日期选择、周期规则切换 |
| 半屏面板（升级引导） | 底部或居中，含权益列表 + 价格 + CTA | 见 2.5 统一模板 |

- 弹窗必须有「关闭/取消」出口（右上 ✕ 或「暂不」），不可强制。
- 遮罩点击默认不关闭（避免误触），以按钮为准。

---

## 二、会员中心页（pages/vip/vip）原型说明

### 2.1 ASCII 线框图（手机屏宽，区块编号）

```
┌────────────────────────────────┐
│  ‹ 返回            会员中心      │  导航栏 (88rpx)
├────────────────────────────────┤
│ ① 会员状态卡                     │
│  ┌──────────────────────────┐  │
│  │ (头像)  微信昵称    [免费]  │  │
│  │ 当前：未开通会员           │  │
│  │ AI 试用   2/2 次 · 已用完  │  │
│  │ 导入解析  8/20 次          │  │
│  │ 媒体空间 ▓▓▓░░░ 128MB/500MB│  │
│  │        [ 立即开通会员 ]    │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ② 核心卖点 Banner               │
│  ┌──────────────────────────┐  │
│  │ ✨ AI 帮你省钱 · 自定义周期 │  │
│  │    场景影像 · 5GB 媒体空间  │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ③ 权益对比（免费 vs 会员）       │
│  ┌──────────────────────────┐  │
│  │ 权益        免费     会员  │  │
│  │ ─────────────────────────│  │
│  │ AI 建议   2次/月   30次/月 │  │
│  │ 文件导入 20次/月    不限次 │  │
│  │ CSV/Excel    ✗        ✓  │  │
│  │ 媒体空间   500MB     5GB  │  │
│  │ 多周期对比    ✗        ✓  │  │
│  │ 分享卡片      ✗        ✓  │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ④ 套餐选择（三档）               │
│  ┌───────────┐ ┌───────────┐  │
│  │  月卡      │ │  年卡 ★推荐 │  │
│  │  ¥8/月     │ │  ¥58/年    │  │
│  │ 连续包月¥6 │ │ 约¥4.8/月  │  │
│  └───────────┘ └───────────┘  │
│  ┌───────────────────────────┐ │
│  │ 终身卡(早鸟)  ¥98  ⚡限时   │ │
│  └───────────────────────────┘ │
├────────────────────────────────┤
│ ⑤ 购买须知 ›                    │
│ ⑥ 订单记录 ›                    │
│                                │
├────────────────────────────────┤
│ ⑦ [ 立即开通 年卡 ¥58 ]（固定）  │  底部固定栏
└────────────────────────────────┘
```

### 2.2 逐区块说明

#### ① 会员状态卡

| 项 | 内容 |
| --- | --- |
| 区块名称 | 会员状态卡 |
| 元素明细 | 头像（40×40rpx 圆角）、昵称、会员徽章（免费 `--text-3` / 会员金 `#C9A063` / 已过期 `--warning`）；主文案「当前：未开通会员 / 有效期至 …」；三项额度行（AI 试用、导入解析、媒体空间进度条）；主按钮「立即开通会员」 |
| 样式要点 | 白卡片、24rpx 圆角；额度行用图标 + 文案 + 右对齐数值；媒体空间用主色进度条（`#00B578` 填充、`#E6F7F0` 底）；接近上限（≥80%）进度条变 `#FF8A00` |
| 交互行为 | 点击头像/昵称：未登录时拉起微信登录；点击「立即开通」：滚动定位/聚焦到套餐区 ④ 并选中默认档；点击额度行：媒体行跳转「我的-空间占用」说明，AI/导入行 Toast 提示权益说明 |
| 埋点 | 页面曝光 `vip_view`（属性：`member_level`、`page_entry`） |

#### ② 核心卖点 Banner

| 项 | 内容 |
| --- | --- |
| 区块名称 | 核心卖点 Banner |
| 元素明细 | 星标 ✨ + 主文案「AI 帮你省钱 · 自定义周期」+ 副文案「场景影像 · 5GB 媒体空间」；背景主色渐变（`#00C784 → #00A86B`）或主色浅底 |
| 样式要点 | 全宽卡片、24rpx 圆角、白色文字（渐变底）；右侧可配一张插画或省略 |
| 交互行为 | 点击整体跳转套餐区 ④（锚点滚动），不额外跳转 |
| 埋点 | 计入 `vip_view`；点击可另埋 `vip_banner_click`（非强制） |

#### ③ 权益对比表

| 项 | 内容 |
| --- | --- |
| 区块名称 | 权益对比表 |
| 元素明细 | 表头「权益 / 免费 / 会员」；6 行权益：AI 建议（2 次/月 vs 30 次/月）、文件导入（20 次/月 vs 不限次）、CSV/Excel 导入（✗/✓）、媒体空间（500MB/5GB）、多周期对比（✗/✓）、分享卡片（✗/✓） |
| 样式要点 | 白卡片；会员列用主色浅底 `#E6F7F0` 高亮 + 会员金 ✓；免费列 ✗ 用 `--text-4`；行高 80rpx；表头用 `--text-3` |
| 交互行为 | 纯展示；权益内容由服务端 `FR-VIP-01` 权益开关下发，前端统一渲染（可随时调整不发布新版本） |
| 埋点 | 无独立事件，随页面曝光 |

#### ④ 套餐选择（三档）

| 项 | 内容 |
| --- | --- |
| 区块名称 | 套餐选择 |
| 元素明细 | 三档卡片——**月卡**：¥8/月，附「连续包月 ¥6/月」副选项；**年卡**：¥58/年、约 ¥4.8/月，角标「★ 推荐」+「立省 ¥38」；**终身卡（早鸟）**：¥98，角标「⚡ 限时」，附「上线 3 个月后下架」说明 |
| 样式要点 | 选中态：主色描边 `#00B578` + 浅底 + 右上角 ✓；默认选中「年卡」（主打档）；「推荐/限时」角标用强调金 `#F7B500`；终身卡显示倒计时/限时标签 |
| 交互行为 | 点击档位切换选中态，同步底部按钮文案；月卡内「连续包月」为独立可切换选项（默认不勾选，不做诱导）；选中后 `pay_start` 的 `product_code` 取 `month` / `month_auto` / `year` / `lifetime` |
| 埋点 | 点击档位 `pay_start`（属性：`product_code`、`price`、`channel`） |

#### ⑤ 购买须知

| 项 | 内容 |
| --- | --- |
| 区块名称 | 购买须知 |
| 元素明细 | 列表项「购买须知 ›」；展开/跳转后展示：自动续费规则、退订方式（微信支付管理/苹果订阅管理）、到期不删数据承诺、iOS 同价说明 |
| 样式要点 | 列表单元样式，右侧箭头，点击跳转 `pages/settings` 子页或半屏说明 |
| 交互行为 | 点击进入须知详情页（静态文案，含《会员服务协议》《自动续费协议》《隐私政策》入口） |
| 埋点 | 无 |

#### ⑥ 订单记录

| 项 | 内容 |
| --- | --- |
| 区块名称 | 订单记录 |
| 元素明细 | 列表项「订单记录 ›」，展示最近一条订单摘要（商品名 + 金额 + 时间 + 状态） |
| 样式要点 | 列表单元，右侧箭头；无订单时显示「暂无订单」灰字 |
| 交互行为 | 点击进入订单列表（商品、金额、渠道、状态、时间）；对「待支付/已关闭」订单提供「继续支付」 |
| 埋点 | 无（订单页可复购时触发 `pay_start`） |

#### ⑦ 底部固定栏

| 项 | 内容 |
| --- | --- |
| 区块名称 | 底部固定开通栏 |
| 元素明细 | 主按钮，文案随选中档位变化，如「立即开通 年卡 ¥58」；左侧可显示「已选：年卡」小字 |
| 样式要点 | 白底 + 上分割线 `--line` + 安全区适配（iPhone 底部 home indicator）；按钮主色胶囊、96rpx 高 |
| 交互行为 | 点击进入确认支付弹窗（2.4 流程）；会员有效期内按钮变为「续费 / 管理订阅」 |
| 埋点 | 点击 `pay_start`（属性：`product_code`、`price`、`channel`） |

### 2.3 四种页面状态

#### 状态 A：未登录

| 项 | 内容 |
| --- | --- |
| 状态卡文案 | 头像占位灰圈 + 「点击登录」；主文案「登录后可查看会员状态」；额度三行隐藏；按钮「微信一键登录」 |
| 差异 | 不展示具体额度与到期时间；套餐区与权益对比仍可浏览，但点击开通先拉起登录 |
| 交互 | 点击任意「开通/登录」→ `wx.login` 授权登录 → 刷新状态卡 |

#### 状态 B：免费用户

| 项 | 内容 |
| --- | --- |
| 状态卡文案 | 徽章「免费」（`--text-3`）；主文案「当前：未开通会员」；额度三行展示——「AI 试用 2/2 次 · 本月已用完」「导入解析 8/20 次」「媒体空间 ▓▓▓░░░ 128MB / 500MB」；按钮「立即开通会员」 |
| 差异 | 进度条/剩余次数实时展示；AI 或导入用尽时该项数字变 `#F5483B` 并附「升级解锁」文字按钮 |
| 交互 | 点击「立即开通」→ 定位套餐区默认选中「年卡」 |

#### 状态 C：会员有效（含到期时间显示）

| 项 | 内容 |
| --- | --- |
| 状态卡文案 | 徽章「会员」（会员金 `#C9A063`）；主文案「有效期至 2026-08-15」；额度三行——「AI 建议 5/30 次 · 本月」「媒体空间 1.2GB / 5GB」「导入解析 不限次」；按钮「续费」 |
| 差异 | 终身卡用户主文案显示「永久有效（终身卡）」，无到期日、无续费按钮，改为「已开通终身会员」；到期前 7 天内主文案旁显示「即将到期」橙色角标 |
| 交互 | 「续费」→ 定位套餐区；点击「管理订阅」→ 跳微信支付/苹果订阅管理页（跳转链接按平台下发） |
| 埋点 | 到期前展示角标时上报 `vip_expire`（`days_left`）；点击续费 `pay_start` |

#### 状态 D：会员已过期

| 项 | 内容 |
| --- | --- |
| 状态卡文案 | 徽章「已过期」（`--warning`）；主文案「已于 2026-08-01 到期」；副文案「历史数据已完整保留，续费后立即恢复全部会员权益」；额度三行展示免费额度（AI 2/2、导入 20/20、媒体 500MB 但已占用提示）；按钮「立即续费」 |
| 差异 | 明确「不删数据」安抚文案（对应 FR-VIP-08）；媒体已超 500MB 时显示「已用 2.1GB / 500MB」红字 +「续费恢复 5GB」 |
| 交互 | 点击「立即续费」→ 定位套餐区 |
| 埋点 | 进入时上报 `vip_expire`（属性：`expired_days`）；续费成功上报 `vip_renew` |

### 2.4 购买流程与异常场景

**标准购买流程**

```
选择档位(④) ──► 点击「立即开通」(⑦)
     └─► 确认支付弹窗：套餐名+价格+自动续费说明+协议勾选
          ├─ 未勾选协议：确认按钮禁用，不可支付
          └─ 勾选协议 → 点击「确认支付」
               ├─ 安卓：wx.requestPayment(微信支付)
               └─ iOS ：wx 虚拟支付 / 苹果 IAP（wx.requestVirtualPayment）
                    └─► 支付成功回调 ──► 服务端 /pay/notify 或 /pay/iap-verify
                         ├─ 验签成功 → 开通/续期会员 → 刷新状态卡
                         └─ 开通成功页(居中弹窗/半屏) → 「开始使用」
```

**确认支付弹窗文案（示例）**

```
┌───────────────────────────┐
│        确认开通            │
│                           │
│  年卡会员                  │
│  ¥58 / 年（约 ¥4.8/月）     │
│  立省 ¥38 · 无限 AI 建议    │
│                           │
│  ☐ 我已阅读并同意           │
│    《会员服务协议》         │
│    《自动续费协议》         │
│                           │
│  [ 暂不 ]   [ 确认支付 ¥58 ]│
└───────────────────────────┘
```

- 选「连续包月」时弹窗内额外展示：「每月自动扣费 ¥6，可随时在『微信支付-扣费服务』取消」。
- 确认支付按钮未勾选协议时为禁用态。

**异常场景处理**

| 场景 | 处理 |
| --- | --- |
| 用户取消支付 | 支付返回 `cancel` → 关闭弹窗，保留原选中档位，Toast「已取消支付」；不重置选中态，用户可直接再次发起 |
| 回调延迟（支付成功但前端未收到） | 前端发起订单状态轮询（间隔 2s → 5s → 10s，共 3 次）调用 `/membership/me` 与订单查询；轮询命中「已支付」则直接刷新会员态，弹「开通成功」 |
| 重复点击防抖 | 「确认支付」按钮点击后立即置为 loading + 禁用，800ms 内忽略重复点击；下单接口以业务 `order_no` 幂等，重复下单返回同一支付参数 |
| 支付成功但回调丢失（补单） | ① 前端：轮询 + 页面「onShow」主动查 `/membership/me`；② 后端：定时任务扫「待支付」订单调微信/苹果查询，主动补开会员；③ 用户侧：订单记录提供「我已支付，点此恢复」按钮触发补单接口 |
| iOS 票据校验失败 | Toast「校验失败，请重试」；订单记录提供「恢复购买（Restore Purchases）」调 `/pay/iap-verify` 重新校验并恢复会员 |
| 支付失败 | Toast「支付失败，请重试」；弹窗按钮变「重新支付」；埋点记录 `pay_start` 失败原因 |
| 网络异常/超时 | Toast「网络异常，请稍后重试」+「重试」文字按钮；不关闭流程上下文 |

### 2.5 升级引导弹窗统一模板

> 供统计页、AI 入口、导入 CSV/Excel 入口、分享卡片、多周期对比等所有「免费用户触发会员权益」的场景复用。

**结构**：标题 + 权益亮点（3~4 条，图标 + 文案）+ 价格卡（默认推荐年卡）+ 主按钮 + 次按钮 + 信任副文案。

**可复用文案示例**

```
┌─────────────────────────────┐
│        ✨ 解锁完整记账体验     │
│                             │
│   ✓ AI 智能省钱建议  每月30次 │
│   ✓ 媒体空间升级到 5GB       │
│   ✓ CSV/Excel 账单导入       │
│   ✓ 多周期对比 · 分享卡片     │
│                             │
│  ┌───────────────────────┐  │
│  │ 年卡 ¥58/年  立省 ¥38  │  │
│  │ ★ 推荐 · 约 ¥4.8/月    │  │
│  └───────────────────────┘  │
│                             │
│  [ 立即开通 ]                │
│  [ 暂不，先看看 ]            │
│                             │
│  随时可取消 · 到期不删数据    │
└─────────────────────────────┘
```

**字段可替换规则**：标题、权益条数（2~4 条）、价格卡（档位/价格/角标）、主按钮文案（「立即开通 / 立即升级 / 解锁并生成」）、次按钮文案（「暂不 / 继续试用 / 取消」）均可按触发场景微调，但结构、信任副文案保持统一。

---

## 三、统计页（pages/stats/stats）原型说明

### 3.1 ASCII 线框图（标注 V1.1 AI 分析入口）

```
┌────────────────────────────────┐
│  ‹ 返回            统计          │  导航栏
├────────────────────────────────┤
│ ① 周期切换器                     │
│  ┌  发薪月 │ 自然月 │ 周  ┐       │  ← 周期规则 Tab
│  ◀  2026-07-25 ~ 2026-08-24  ▶ │  ← 周期实例左右滑
│           [ 日期选择 ▾ ]        │
├────────────────────────────────┤
│ ② 周期总览卡                     │
│  ┌──────────────────────────┐  │
│  │ 总支出 ¥3,286.50  总收入 ¥8,000.00│
│  │ 结余  +¥4,713.50  日均 ¥117.38 │
│  │ 记账 28 天                 │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ③ ✨ AI 帮你分析本期消费    ›    │  ← V1.1 AI 入口（会员/试用）
├────────────────────────────────┤
│ ④ 分类占比（环形图 + 排行）       │
│  ┌──────────────────────────┐  │
│  │      ◯     餐饮 42% ¥1,380 │  │
│  │  (环形图)   交通 18%  ¥592  │  │
│  │  支出 ¥3286 │购物 15%  ¥493  │  │
│  │  [一级▾]   居住  8%  ¥263  │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ⑤ 周期内趋势                     │
│  ┌──────────────────────────┐  │
│  │  每日支出折线/柱状图        │  │
│  │  ── 平均线 · ● 最高日       │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ⑥ 周期对比（会员权益）            │
│  ┌──────────────────────────┐  │
│  │ 本期 vs 上期   支出 -8.2%  │  │
│  │ 结余 +¥320     [升级解锁]  │  │
│  └──────────────────────────┘  │
├────────────────────────────────┤
│ ⑦ 分享卡片（会员权益）            │
│  ┌──────────────────────────┐  │
│  │ 生成我的周期总结    [分享]  │  │
│  └──────────────────────────┘  │
└────────────────────────────────┘
```

### 3.2 区块说明

#### ① 周期切换器（FR-STA-06）

| 项 | 内容 |
| --- | --- |
| 功能 | 周期规则切换 + 周期实例左右滑动 + 日期选择 |
| 元素明细 | ① 规则 Tab（横向，展示启用规则名，如「发薪月/自然月/周」，最多 5 个，超出横滑）；② 实例条（当前规则名 + 起止日期，左右箭头/`swiper` 左右滑切换相邻实例）；③ 「日期选择」入口 |
| 样式要点 | 规则 Tab 选中下划线主色；实例条居中显示「2026-07-25 ~ 2026-08-24」，箭头为 `--text-3` 圆钮 |
| 交互行为 | 点规则 Tab 切规则（默认展示「当前进行中实例」）；左右滑切相邻实例；点日期弹日历选择器，选中后定位到对应实例/自定义区间 |
| 埋点 | `stats_view`（属性：`cycle_rule_type`、`cycle_instance_id`、`duration`） |

#### ② 周期总览卡（FR-STA-01）

| 项 | 内容 |
| --- | --- |
| 功能 | 总收入 / 总支出 / 结余 / 日均支出 / 记账天数 |
| 元素明细 | 2×2 数值网格：总支出（主，特大金额 64rpx）、总收入、结余、日均支出；底部「记账 28 天」 |
| 样式要点 | 总支出用 `--expense`、总收入用 `--income`、结余正绿负红、日均用 `--text-1`；主数字用等宽数字字体 |
| 交互行为 | 点击每个数字下钻明细（见 3.3 表，带类型/周期/日期参数） |
| 埋点 | `stats_view`；下钻另埋 `stats_detail_tap`（可选） |

#### ③ AI 分析入口（V1.1，FR-STA-10）——详见 3.5

#### ④ 分类占比（FR-STA-02）

| 项 | 内容 |
| --- | --- |
| 功能 | 支出分类环形图 + 排行列表 + 一/二级分类切换；收入来源分布 |
| 元素明细 | 环形图（中心显示总支出金额）、右侧/下方排行列表（分类名、金额、占比、进度条）、「一级/二级」切换、「支出/收入」切换 |
| 样式要点 | 环形图各分类用色板区分（top3 用品牌色系，其余用灰阶）；排行条用对应分类色填充；占比保留 1 位小数 |
| 交互行为 | 点击环形图扇区/排行项 → 明细穿透；切「支出/收入」刷新图表；切「一级/二级」钻取分类层级 |
| 埋点 | `stats_view`；穿透 `stats_detail_tap`（`category_id`） |

#### ⑤ 周期内趋势图（FR-STA-03）

| 项 | 内容 |
| --- | --- |
| 功能 | 每日支出折线/柱状图，标注最高日与平均线 |
| 元素明细 | 柱状/折线图、平均虚线、最高日高亮点 + 标签「最高 ¥186 · 08-02」 |
| 样式要点 | 图表用 ECharts 小程序版或自研 Canvas；柱色主色，最高日柱色 `--expense`，平均线灰虚线 |
| 交互行为 | 点击单日柱 → 明细穿透（date=当日）；长按显示当日金额 tooltip |
| 埋点 | `stats_view` |

#### ⑥ 周期对比卡（FR-STA-04，会员权益）

| 项 | 内容 |
| --- | --- |
| 功能 | 相邻周期对比（总支出、结余、环比）+ 任意两周期对比 |
| 元素明细 | 本期 vs 上期：总支出、结余、环比涨跌（↑↓ + 百分比，涨红跌绿）；「任意两周期对比」入口 |
| 样式要点 | 环比下降（省钱）用绿、上涨用红；免费用户显示灰化 + 「会员」锁标 + 「升级解锁」按钮 |
| 交互行为 | 会员：点「任意两周期对比」弹周期选择器；免费：点击弹升级引导弹窗（2.5 模板） |
| 埋点 | `stats_view`；升级弹窗触发 `pay_start`（`product_code`） |

#### ⑦ 分享卡片（FR-STA-08，会员权益）

| 项 | 内容 |
| --- | --- |
| 功能 | 生成周期总结卡片（总支出、结余、top3 分类），可隐藏金额，可转发 |
| 元素明细 | 「生成我的周期总结」按钮 + 预览缩略；生成后卡片含：周期区间、总支出、结余、top3 分类（图标+名称+占比）、可勾选「隐藏金额」、小程序码 |
| 样式要点 | 卡片为分享图（Canvas 绘制）；「隐藏金额」时金额显示 `¥***` |
| 交互行为 | 会员：生成 → 预览 → 「转发给好友/保存图片」；免费：点击弹升级引导弹窗 |
| 埋点 | 生成 `stats_share_gen`；升级触发 `pay_start` |

### 3.3 交互规则表（跳转与传参）

> 明细穿透统一跳 `pages/bills/bills`，传参按以下约定（均为 query 参数）。

| 可点击元素 | 跳转目标 | 传参 | 说明 |
| --- | --- | --- | --- |
| 周期规则 Tab | 本页（不跳转） | `cycleRuleId` | 切换统计规则，默认落到「当前进行中实例」 |
| 周期实例左右滑 | 本页（不跳转） | `cycleInstanceId` | 刷新全页各区块数据 |
| 日期选择器 | 本页弹层 | `startDate` / `endDate` | 选中后定位到对应实例或自定义区间 |
| 总览卡「总支出」 | `pages/bills/bills` | `billType=expense` `cycleId` `startDate` `endDate` | 仅该周期支出 |
| 总览卡「总收入」 | `pages/bills/bills` | `billType=income` `cycleId` `startDate` `endDate` | 仅该周期收入 |
| 总览卡「结余/日均」 | `pages/bills/bills` | `cycleId` `startDate` `endDate` | 结余穿透展示全部收支 |
| 环形图扇区 | `pages/bills/bills` | `categoryId` `billType=expense` `cycleId` | 该分类支出明细 |
| 排行列表项 | `pages/bills/bills` | `categoryId` `billType` `cycleId` `level` | `level=1/2` 分类层级 |
| 分类层级切换 | 本页（不跳转） | `level=1\|2` | 图表与排行钻取 |
| 趋势图单日柱 | `pages/bills/bills` | `date=当日` `cycleId` `billType=expense` | 单日支出明细 |
| 周期对比「任意两周期」 | 本页弹层 | `cycleAId` / `cycleBId` | 会员可用 |
| 周期对比「升级解锁」 | 升级引导弹窗 | — | 免费用户触发 |
| 分享卡片「生成」 | 本页生成 | `cycleId` | 会员可用 |
| AI 分析入口 | 分支流程（见 3.5） | `cycleId` | V1.1 |

**返回规则**：从明细页返回统计页时，保留进入前的周期实例与筛选上下文（不重置）。

### 3.4 状态清单

| 状态 | 触发 | 表现与文案 |
| --- | --- | --- |
| 加载骨架屏 | 首次进入/切换周期/下拉刷新 | 灰色骨架块模拟：总览卡 2×2、环形图圆圈、趋势图条块；≤ 2 秒渲染（FR-STA 性能） |
| 空状态（无账单） | 当前周期/筛选区间无任何账单 | 插画 + 主文案「这个周期还没有账单」+ 副文案「记下第一笔，开启你的周期总结」+ 主按钮「去记一笔」（跳 `pages/index/index`）+ 次按钮「导入账单」；分类图/趋势图/对比卡隐藏，仅保留总览卡（显示 ¥0.00）与 AI 入口（灰色不可点，文案「记账后即可分析」） |
| 错误态（加载失败） | 接口失败/网络异常 | 居中插画 + 「数据加载失败」+ 副文案「请检查网络后重试」+ 主按钮「重新加载」；重试重新拉取周期数据 |
| 免费用户 vs 会员差异 | 权益墙 | 周期对比卡、分享卡片为**会员权益**：免费用户显示灰化 + 锁标 +「升级解锁」；点击弹 2.5 升级引导弹窗；AI 入口免费用户显示「剩余 2 次」角标，用尽后显示「次数已用完，升级无限」 |

### 3.5 AI 分析入口（V1.1）

**入口位置与文案**：置于周期总览卡（②）正下方、分类占比（④）上方——即首屏可见的黄金位置（线框图 ③ 区块）。

| 状态 | 入口文案 |
| --- | --- |
| 未授权 AI | 「✨ AI 帮你分析本期消费 ›」 |
| 已授权·会员/有额度 | 「✨ AI 帮你分析本期消费 ›」 |
| 免费·额度用尽 | 「✨ AI 帮你分析本期消费 › 次数已用完」 |

**点击后的分支流程**

```
点击 AI 入口
 ├─ 未登录 → 拉起微信登录 → 回到本流程
 ├─ 未授权 AI（ai_consent=0）→ 授权弹窗
 │     ├─ 同意 → ai_consent=1 → 继续
 │     └─ 拒绝 → ai_consent=2 → Toast「已拒绝，可在设置中开启」
 │              → 提供「去设置」入口（FR-SET-04）
 ├─ 已拒绝（ai_consent=2）→ 提示 + 「去设置开启」按钮
 └─ 已授权 → 校验额度
       ├─ 会员：AI 30 次/月，未用尽 → 跳 pages/ai/ai（cycleId）
       │      已用尽 → Toast「本月 30 次已用完」
       ├─ 免费：AI 2 次/月，未用尽 → 跳 pages/ai/ai（cycleId）
       │      已用尽 → 升级引导弹窗（2.5 模板，文案「AI 试用已用完」）
       └─ 跳转时埋点 ai_view（insight_type、member_level）
```

**授权弹窗文案（示例）**

```
┌───────────────────────────┐
│     开启 AI 智能分析        │
│                           │
│  AI 将读取您的账单数据（收支、│
│  分类、频次、周期）用于生成个 │
│  性化建议。                 │
│  数据仅用于您本人分析，     │
│  不用于训练模型。           │
│                           │
│  [ 暂不 ]   [ 同意并开启 ]  │
└───────────────────────────┘
```

**AI 页相关埋点**（V1.1）：`ai_view`（进入）、`ai_generate`（发起生成，`insight_type`=report/savings/food）、`ai_success`/`ai_fail`（结果，`duration`、失败原因）、`ai_feedback`（有用/无用）。

---

## 四、附录：全部页面文案汇总表

> 下表文案可直接用于 UI 稿；「位置」对应本文区块编号。

### 4.1 会员中心页文案

| 页面/位置 | 文案 | 说明 |
| --- | --- | --- |
| 会员页·导航栏 | 会员中心 | 标题 |
| ① 状态卡·未登录 | 点击登录 / 登录后可查看会员状态 / 微信一键登录 | 未登录态 |
| ① 状态卡·免费 | 免费 / 当前：未开通会员 / 立即开通会员 | 徽章+主文案+按钮 |
| ① 状态卡·会员 | 会员 / 有效期至 2026-08-15 / 续费 | 徽章+主文案+按钮 |
| ① 状态卡·终身 | 会员 / 永久有效（终身卡） / 已开通终身会员 | 终身卡态 |
| ① 状态卡·过期 | 已过期 / 已于 2026-08-01 到期 / 立即续费 | 徽章+主文案+按钮 |
| ① 状态卡·过期副文案 | 历史数据已完整保留，续费后立即恢复全部会员权益 | 安抚文案（FR-VIP-08） |
| ① 额度行 | AI 建议 2/2 次 · 本月已用完 / 导入解析 8/20 次 / 媒体空间 128MB / 500MB | 免费额度 |
| ① 额度行·会员 | AI 建议 5/30 次 · 本月 / 导入解析 不限次 / 媒体空间 1.2GB / 5GB | 会员额度 |
| ② 卖点 Banner | ✨ AI 帮你省钱 · 自定义周期 | 主文案 |
| ② 卖点 Banner | 场景影像 · 5GB 媒体空间 | 副文案 |
| ③ 权益对比·表头 | 权益 / 免费 / 会员 | — |
| ③ 权益对比·行 | AI 建议（2 次/月 / 30 次/月） | — |
| ③ 权益对比·行 | 文件导入（20 次/月 / 不限次） | — |
| ③ 权益对比·行 | CSV/Excel 导入（✗ / ✓） | V1.1 会员权益 |
| ③ 权益对比·行 | 媒体空间（500MB / 5GB） | — |
| ③ 权益对比·行 | 多周期对比（✗ / ✓） | 会员权益 |
| ③ 权益对比·行 | 分享卡片（✗ / ✓） | 会员权益 |
| ④ 套餐·月卡 | 月卡 / ¥8/月 / 连续包月 ¥6/月 | 月卡档 |
| ④ 套餐·年卡 | 年卡 / ¥58/年 / 约 ¥4.8/月 / ★ 推荐 / 立省 ¥38 | 主打档 |
| ④ 套餐·终身卡 | 终身卡（早鸟）/ ¥98 / ⚡ 限时 / 上线 3 个月后下架 | 早鸟档 |
| ⑤ 购买须知 | 购买须知 › | 列表项 |
| ⑤ 须知详情 | 自动续费规则 / 退订方式 / 到期不删数据 / iOS 与安卓同价说明 | — |
| ⑥ 订单记录 | 订单记录 › / 暂无订单 / 继续支付 / 我已支付，点此恢复 | 列表项+态 |
| ⑦ 底部栏 | 立即开通 年卡 ¥58 / 已选：年卡 / 续费 / 管理订阅 | 随选中态变化 |
| 确认弹窗·标题 | 确认开通 | — |
| 确认弹窗·正文 | 年卡会员 / ¥58 / 年（约 ¥4.8/月）/ 立省 ¥38 · 无限 AI 建议 | — |
| 确认弹窗·协议 | 我已阅读并同意《会员服务协议》《自动续费协议》 | 勾选 |
| 确认弹窗·连续包月 | 每月自动扣费 ¥6，可随时在「微信支付-扣费服务」取消 | 选中包月时 |
| 确认弹窗·按钮 | 暂不 / 确认支付 ¥58 | — |
| 开通成功 | 开通成功 / 开始使用 | 成功页 |
| 异常 Toast | 已取消支付 / 支付失败，请重试 / 校验失败，请重试 / 网络异常，请稍后重试 | 异常态 |

### 4.2 统计页文案

| 页面/位置 | 文案 | 说明 |
| --- | --- | --- |
| 统计页·导航栏 | 统计 | 标题 |
| ① 周期切换器 | 发薪月 / 自然月 / 周 / 日期选择 | 规则 Tab（示例） |
| ① 实例条 | 2026-07-25 ~ 2026-08-24 | 实例起止 |
| ② 总览卡 | 总支出 ¥3,286.50 / 总收入 ¥8,000.00 / 结余 +¥4,713.50 / 日均 ¥117.38 / 记账 28 天 | 数值 |
| ③ AI 入口 | ✨ AI 帮你分析本期消费 › | 会员/有额度 |
| ③ AI 入口·用尽 | ✨ AI 帮你分析本期消费 › 次数已用完 | 免费用尽 |
| ④ 分类占比 | 支出 / 收入 / 一级 / 二级 | 切换 |
| ④ 排行 | 餐饮 42% ¥1,380 / 交通 18% ¥592 / 购物 15% ¥493 / 居住 8% ¥263 | 示例 |
| ⑤ 趋势图 | 每日支出 / 最高 ¥186 · 08-02 | 图例+标签 |
| ⑥ 周期对比 | 本期 vs 上期 / 支出 -8.2% / 结余 +¥320 / 任意两周期对比 / 升级解锁 | 会员权益 |
| ⑦ 分享卡片 | 生成我的周期总结 / 分享 / 隐藏金额 | 会员权益 |
| 骨架屏 | （无文字） | 加载态 |
| 空状态 | 这个周期还没有账单 / 记下第一笔，开启你的周期总结 / 去记一笔 / 导入账单 | 无账单 |
| 空状态·AI | 记账后即可分析 | AI 入口灰化 |
| 错误态 | 数据加载失败 / 请检查网络后重试 / 重新加载 | 失败 |

### 4.3 通用组件文案

| 位置 | 文案 | 说明 |
| --- | --- | --- |
| 升级引导弹窗·标题 | ✨ 解锁完整记账体验 | 统一模板 |
| 升级引导·权益 | ✓ AI 智能省钱建议 每月30次 / ✓ 媒体空间升级到 5GB / ✓ CSV/Excel 账单导入 / ✓ 多周期对比 · 分享卡片 | 4 条 |
| 升级引导·价格卡 | 年卡 ¥58/年 立省 ¥38 / ★ 推荐 · 约 ¥4.8/月 | 默认推荐年卡 |
| 升级引导·按钮 | 立即开通 / 暂不，先看看 | — |
| 升级引导·信任 | 随时可取消 · 到期不删数据 | — |
| AI 授权弹窗·标题 | 开启 AI 智能分析 | — |
| AI 授权弹窗·正文 | AI 将读取您的账单数据（收支、分类、频次、周期）用于生成个性化建议。数据仅用于您本人分析，不用于训练模型。 | — |
| AI 授权弹窗·按钮 | 暂不 / 同意并开启 | — |
| AI 拒绝提示 | 已拒绝，可在设置中开启 / 去设置 | — |
| AI 额度用尽 | 本月 30 次已用完 / AI 试用已用完 | 会员/免费 |

### 4.4 埋点事件速查

| 事件 | 关键属性 | 触发点 |
| --- | --- | --- |
| `vip_view` | `member_level`、`page_entry` | 会员页曝光 |
| `pay_start` | `product_code`（month/month_auto/year/lifetime）、`price`、`channel`（wxpay/iap） | 选档/点开通/确认支付 |
| `pay_success` | `order_no`、`product_code`、`price`、`channel` | 支付成功回调 |
| `vip_expire` | `days_left` / `expired_days` | 到期角标曝光/过期态进入 |
| `vip_renew` | `product_code`、`result` | 续费成功 |
| `stats_view` | `cycle_rule_type`、`cycle_instance_id`、`duration` | 统计页曝光/切换 |
| `stats_detail_tap` | `category_id`、`bill_type`、`level` | 明细穿透 |
| `stats_share_gen` | `cycle_id` | 分享卡片生成 |
| `ai_view` | `insight_type`、`member_level` | AI 入口点击 |
| `ai_generate` / `ai_success` / `ai_fail` | `insight_type`、`duration`、失败原因 | AI 生成链路 |
| `ai_feedback` | `insight_type`、`feedback` | 建议反馈 |
