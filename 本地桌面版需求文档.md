# 「铭记日常」本地桌面版 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 铭记日常本地桌面版产品需求文档 |
| 产品名称 | 铭记日常（桌面版） |
| 文档版本 | V1.1 |
| 文档状态 | 待评审 |
| 目标平台 | Windows 10（21H2+）/ Windows 11，64 位 |
| 产品形态 | 本地单机桌面软件（离线可用，无账号体系） |
| **分发方式** | **① 便携版 zip（解压即用）② 安装版 exe（向导安装）**，均自包含、无需任何运行时 |
| 关联文档 | 《记账小程序产品需求文档.md》V1.2（功能基准）；《AI功能设计-Prompt与接口.md》（AI 能力复用） |
| 开发定位 | 小程序开发前的**初版验证与自用测试版**，先跑通核心功能与分发链路 |

---

## 1. 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
| --- | --- | --- | --- |
| V1.0 | 2025-01-XX | 产品 | 首版：本地版需求，含双分发方式（zip/exe）规格 |
| V1.1 | 2025-01-XX | 产品 | 评审确认：产品名「铭记日常」、图标（账本+M）、AI=自备 Key 可开关、技术栈=Tauri 2（Rust+Vue3+TS） |

---

## 2. 项目概述

### 2.1 背景与目标

1. 小程序版开发周期长（约 18 周全职）且依赖云服务、支付进件、AI 备案等不可控环节；
2. 先做**本地桌面版**：功能与小程序版对齐，**自己先用起来、邀请朋友测试**，验证核心四功能（自定义周期、导入识别、周期统计、媒体记录）的产品价值，沉淀可复用的解析规则、统计口径、AI Prompt；
3. 硬性要求：**两种分发方式都要能让没有开发环境的普通人直接使用**——解压 zip 双击即用、exe 安装包一键安装。

### 2.2 产品定位

**一款数据完全保存在本机、离线可用、免安装/易安装的个人记账桌面软件**，作为小程序版的初版载体与测试平台。

与小程序版的关系：
- **功能对齐**：四类核心需求（周期、导入、统计、媒体）口径一致；
- **能力复用**：导入解析规则、AI Prompt 模板、分类体系、统计公式直接复用；
- **差异点**：无登录/云同步/会员支付，数据 100% 本地，AI 可选（用户自备 Key）。

### 2.3 成功标准（V1.0）

| 维度 | 标准 |
| --- | --- |
| 自用验证 | 作者本人连续记账 2 周，四功能日常使用无障碍 |
| 分发验证 | 至少 3 位无技术背景朋友通过 zip/exe 两种方式成功安装并完成一笔记账 |
| 数据安全 | 测试期 0 起数据丢失/损坏事件（自动备份兜底） |
| 性能 | 1 万条账单内明细滚动流畅、统计页 ≤ 2 秒 |

---

## 3. 用户与使用场景

| 用户 | 场景 |
| --- | --- |
| 作者本人 | 日常记账、周期总结、导入历史账单、测试全部功能 |
| 朋友（无技术背景） | 收到 zip/exe → 打开 → 记一笔 → 看统计；遇到问题直接反馈 |
| 测试关注点 | 换电脑可用、重装系统后数据可迁移、断网可用 |

---

## 4. 术语定义

沿用小程序 PRD：账单（Bill）、记账周期（Cycle）、周期规则（Cycle Rule）、周期实例（Cycle Instance）、账本（Book，本地版为单账本）、导入任务（Import Task）、媒体附件（Media）、AI 洞察（AI Insight）。

新增术语：

| 术语 | 定义 |
| --- | --- |
| 便携版（Portable） | zip 解压即可运行、数据随应用目录保存的形态 |
| 安装版（Installed） | exe 安装向导安装、数据保存到用户数据目录的形态 |
| 数据目录（Data Dir） | 保存数据库与媒体文件的目录，两种形态位置不同（见 8.1/8.2） |
| 自包含（Self-contained） | 目标机器无需安装任何运行时（Node/.NET 等）即可运行 |

---

## 5. 范围界定

### 5.1 V1.0 范围内

- 手工记账（快记 + 完整记账、分类、账户）
- 自定义记账周期管理（预设周/月 + 完全自定义）
- 账单导入与识别（txt / md / docx / 粘贴文本；规则解析优先，AI 解析可选）
- 周期汇总统计（总览、分类占比、趋势、周期对比、明细穿透）
- 照片/视频媒体附件（本地存储）
- 数据管理（备份、恢复、导出 CSV/JSON、数据目录迁移）
- 设置（AI Key 配置、数据目录、主题、关于）
- **两种分发形态（zip 便携版 + exe 安装版）的完整打包流水线**

### 5.2 V1.0 明确不包含

| 不包含项 | 说明 |
| --- | --- |
| 账号登录 / 云同步 / 多端 | 本地单机；未来通过"导出→导入小程序"或云同步打通（V1.1+） |
| 会员 / 支付 | 本地版免费，仅供自用与测试 |
| AI 智能建议（省钱/饮食/报告） | 列入本地版 V1.1（可选模块，需用户自备 DeepSeek Key）；V1.0 仅保留导入的 AI 解析兜底（可选开启） |
| 自动更新 | V1.0 手动替换/重装（数据保留）；自动更新 P2 |
| 代码签名 | 后续购买证书再签（见 8.4） |
| macOS / Linux | 仅 Windows（后续按需） |
| 多人账本、预算、语音记账 | 与小程序版同步列入后续 |

### 5.3 与小程序版的功能对照（V1.0 本地版）

| 小程序版功能 | 本地版 V1.0 | 差异说明 |
| --- | --- | --- |
| 手工记账 | ✅ | 无登录；桌面键盘输入更高效 |
| 自定义周期 | ✅ | 规则引擎完全一致 |
| 导入 txt/md/docx/粘贴 | ✅ | 本地解析；AI 兜底需自备 Key |
| 周期统计 | ✅ | 口径一致；无分享卡片（P2） |
| 媒体照片/视频 | ✅ | 存本地数据目录，无 500MB/5GB 配额 |
| 会员体系 | ❌ | 本地版不涉及 |
| AI 建议三功能 | ⏳ V1.1 | 可选模块 + 自备 Key |

---

## 6. 总体架构与页面结构

### 6.1 应用形态

单窗口桌面应用，左侧导航 + 右侧内容区：

```
┌──────────┬──────────────────────────────┐
│ 导航      │ 内容区                         │
│  记一笔   │  （对应页面）                   │
│  明细     │                               │
│  统计     │                               │
│  导入     │                               │
│  数据管理 │                               │
│  设置     │                               │
└──────────┴──────────────────────────────┘
```

页面：记一笔（首屏）、明细（流水）、统计（周期汇总）、导入（文件/粘贴→预览→入库）、数据管理（备份/恢复/导出/数据目录）、设置（AI Key、主题、关于）。

### 6.2 数据存储方案

- **数据库**：SQLite 单文件（`data/app.db`），WAL 模式，崩溃自动恢复；
- **媒体文件**：`data/media/{yyyyMM}/{mediaId}.{ext}`，缩略图 `data/media/thumb/`；
- **配置**：`data/config.json`（主题、AI Key 等）；
- **备份**：`data/backups/`（自动备份 zip）。

### 6.3 离线与联网原则（隐私红线）

- 软件**默认零网络请求**，断网下所有功能可用；
- 仅两处主动联网（均在设置中显式开启）：① AI 解析/建议（调用 DeepSeek，需用户自备 Key）；② 检查更新（P2）；
- 联网行为在设置页有开关与说明；隐私声明写明"账单数据不上传，仅 AI 功能按需向 DeepSeek 发送聚合统计数据"。

---

## 7. 功能需求详述

> 优先级：**P0** 必须有；**P1** 应有；**P2** 可选/后置。

### 7.1 基础记账

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-01 | **记一笔**：金额（必填）、收支方向、分类（两级，记忆上次）、账户（记忆上次）、日期（默认今天）、备注、媒体附件；键盘快捷键（如 Enter 保存、Esc 取消） | P0 |
| FR-L-02 | **快记**：首屏焦点直接落在金额输入框，一次交互完成"金额→分类→保存" | P0 |
| FR-L-03 | **明细页**：按日分组流水、筛选（分类/账户/收支/日期区间）、搜索备注；1 万条内滚动流畅 | P0 |
| FR-L-04 | **账单编辑/删除**：删除二次确认，联动删除媒体文件 | P0 |
| FR-L-05 | **分类/账户管理**：内置分类（附录 B 同小程序版）、自定义增改停用；内置现金/微信/支付宝/银行卡 + 自定义 | P0 |

**验收**：完整记一笔 ≤ 30 秒（桌面键盘）；删除账单后媒体文件在磁盘同步删除。

### 7.2 记账周期管理

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-10 | **周期规则引擎**：预设按周（周起始可选）/按月（月起始 1~28 日）+ 完全自定义（起始日 + N 天/N 周/N 月 + 结束方式）；多条规则并存（≤5 条）；规则与小程序版逻辑完全一致（含 28 日短月顺延、含头含尾边界） | P0 |
| FR-L-11 | **账单自动归属**：按记账日期落入全部启用规则的周期实例；改日期自动重算 | P0 |
| FR-L-12 | **周期管理页**：规则增改停删；编辑器实时预览最近 3 个周期起止日 | P0 |

**验收**：与小程序版测试用例（7.1.5）同一套用例全部通过。

### 7.3 账单导入与智能识别

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-20 | **文件类型与大小**：.txt/.md ≤ 1MB、.docx ≤ 5MB + 粘贴文本；.doc 提示另存为 .docx | P0 |
| FR-L-21 | **选择文件**：本地文件选择器（桌面特性，比小程序更自由） | P0 |
| FR-L-22 | **解析引擎**：本地规则解析（附录 A 模板，与小程序版同一套规则）；docx 本地解包（mammoth）；**AI 语义解析兜底（可选）**——设置中配置 DeepSeek Key 后开启，未配置则纯规则解析并提示"配置 AI 可提升自由文本识别率" | P0 |
| FR-L-23 | **预览确认**：逐条编辑/删除/忽略；低置信行标红；全选/全不选；确认批量入库；导入任务 24 小时内可撤销 | P0 |
| FR-L-24 | **重复防护**：文件内容 MD5 去重提示 | P1 |
| FR-L-25 | **失败兜底**：无法解析的行保留原文生成草稿（金额待填） | P1 |

**验收**：附录 A 标准格式准确率 ≥ 95%；自由文本（开启 AI 时）≥ 85%；100 条解析 ≤ 10 秒；未配置 AI 时纯规则可用且功能不中断。

### 7.4 周期汇总统计

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-30 | **周期总览**：总收入/总支出/结余/日均支出/记账天数 | P0 |
| FR-L-31 | **分类汇总**：支出分类环形图 + 排行（一/二级切换）、收入分布 | P0 |
| FR-L-32 | **周期内趋势**：每日支出折线/柱状（标注最高日与平均线） | P1 |
| FR-L-33 | **周期对比**：相邻周期环比 + 任意两周期对比 | P1 |
| FR-L-34 | **明细穿透**：点击统计项跳转明细页并带筛选条件 | P0 |
| FR-L-35 | **周期切换器**：规则切换 + 周期实例前后翻页 + 日期直达 | P0 |

**验收**：统计口径与小程序版 7.3 一致；对账测试通过；无数据周期显示空状态。

### 7.5 媒体附件（照片/视频）

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-40 | **添加媒体**：记账/编辑时选择本地照片/视频文件（或拖拽进窗口）；单笔最多 9 照片 + 1 视频 | P0 |
| FR-L-41 | **本地存储**：文件复制进数据目录（不引用原路径，防止原文件移动后失效）；生成缩略图（照片 ≤ 长边 640px；视频取首帧封面） | P0 |
| FR-L-42 | **查看**：账单详情九宫格；照片点击放大预览、视频调起本地播放 | P0 |
| FR-L-43 | **删除与清理**：单删媒体；删账单联动删除；数据管理页提供"孤儿文件清理" | P1 |
| FR-L-44 | **体积提示**：设置页展示媒体总占用；数据目录磁盘不足时提示 | P1 |

**验收**：删除账单后磁盘文件同步删除；原文件移动/删除后软件内媒体仍可查看（已复制）。

### 7.6 数据管理（本地版特有，安全底线）

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-50 | **自动备份**：每次正常退出前自动备份数据库到 `data/backups/`（zip，保留最近 7 份）；程序崩溃后下次启动校验数据库完整性并提示恢复 | P0 |
| FR-L-51 | **手动备份/恢复**：一键备份整个数据目录为 zip（可选含媒体）；从备份 zip 恢复（覆盖前二次确认） | P0 |
| FR-L-52 | **导出**：全部账单导出 CSV（与小程序版字段一致）；高级导出 JSON（含分类/账户/周期规则，用于迁移） | P0 |
| FR-L-53 | **数据目录**：设置页展示数据目录位置、一键打开目录；支持"迁移数据目录"（便携↔安装互通，见 8.3） | P1 |
| FR-L-54 | **升级兼容**：新版本首次启动自动迁移旧数据库结构（schema 版本号 + 迁移脚本） | P0 |

**验收**：模拟强杀进程后重启，数据无丢失且自动恢复；备份 zip 在另一台电脑恢复后数据完整一致。

### 7.7 设置

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-L-60 | **AI 配置**：DeepSeek API Key 输入（本地保存于 config.json，仅本机）、模型/参数默认值、连接测试按钮、AI 功能总开关（**默认关闭，用户可随时开启/关闭**） | P1 |
| FR-L-61 | **外观**：浅色/深色主题（跟随系统可选） | P2 |
| FR-L-62 | **关于**：版本号、使用说明（含"如何发给朋友"指引）、问题反馈方式 | P1 |

### 7.8 AI 智能建议（本地版 V1.1，规划）

- 复用小程序版 7.7 的三类能力（周期报告/省钱建议/饮食推荐）与《AI功能设计-Prompt与接口.md》的 Prompt；
- 本地版特性：需用户自备 Key；调用前明示"将向 DeepSeek 发送聚合统计数据"；结果本地缓存；频次不做强制限制（自备 Key 成本自负），但提供日用量提示；
- V1.0 仅实现导入解析的 AI 兜底（FR-L-22），三功能列 V1.1。

---

## 8. 分发与部署需求（V1.0 核心硬要求）

> 本章是本版与小程序版差异最大的部分，**两个分发形态均为一等公民**，任一形态不达标 V1.0 不验收。

### 8.1 便携版 zip 规格

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-DIS-01 | **产物**：`铭记日常-便携版-v1.0.0-win-x64.zip`；解压后目录含 `铭记日常.exe`（前端资源已内嵌为单文件），**自包含**——依赖系统内置 WebView2（Win10 21H2+/Win11 已内置，见 8.4） | P0 |
| FR-DIS-02 | **解压即用**：解压到任意路径（含中文/带空格路径）后双击 exe 即启动，无需管理员权限、不写注册表、不残留系统文件 | P0 |
| FR-DIS-03 | **数据位置**：数据目录位于**程序同目录** `./data/`（首次运行自动创建）；整个文件夹复制到 U 盘/新电脑即可带走全部数据 | P0 |
| FR-DIS-04 | **分发包不含数据**：打出的 zip 内不包含 `data/` 目录（避免作者数据外泄），接收者首次运行自动生成干净数据 | P0 |
| FR-DIS-05 | **异常路径防护**：若检测到程序运行在压缩包临时目录或只读目录（如未解压直接双击、解压在 Program Files），弹窗提示"请先完整解压到可写目录"并将数据目录回退到 `%APPDATA%\铭记日常`，防止数据写入临时目录丢失 | P0 |
| FR-DIS-06 | **卸载=删除文件夹**：便携版无卸载程序，删除整个文件夹即完全卸载（在说明文档写明） | P0 |

**验收**：在一台全新、未装任何开发环境的 Windows 电脑上，解压→双击→记一笔→重启软件数据仍在；路径含中文与空格通过。

### 8.2 安装版 exe 规格

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-DIS-10 | **产物**：`铭记日常-Setup-1.0.0.exe`（NSIS 安装器，含安装向导，安装时检测并引导 WebView2 运行时） | P0 |
| FR-DIS-11 | **每用户安装**：默认安装到 `%LOCALAPPDATA%\Programs\铭记日常`（**无需管理员权限**，避免 UAC 弹窗吓退普通用户）；提供自定义安装路径 | P0 |
| FR-DIS-12 | **快捷方式**：创建桌面与开始菜单快捷方式；可选开机自启（默认关） | P0 |
| FR-DIS-13 | **数据位置**：数据目录在 `%APPDATA%\铭记日常\`（与程序目录分离，升级/卸载不丢数据） | P0 |
| FR-DIS-14 | **卸载**：提供卸载器（控制面板可卸载），卸载时**询问是否保留数据**；默认保留数据目录 | P0 |
| FR-DIS-15 | **覆盖升级**：同版本号重装、新版本覆盖安装均保留数据（读取现有数据目录） | P0 |

**验收**：标准用户（非管理员）账户下安装、使用、卸载全流程通过；卸载重装后账单数据仍在。

### 8.3 双形态数据互通

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-DIS-20 | **迁移规则**：两种形态的数据目录结构完全一致（`app.db` + `media/` + `backups/`），整体拷贝即可互通 | P0 |
| FR-DIS-21 | **迁移入口**：设置页"迁移数据目录"功能——便携版可选迁移到 `%APPDATA%`（或反之），迁移后数据可被安装版读取 | P1 |
| FR-DIS-22 | **说明文档**：zip 内附带《使用说明.txt》：双形态差异、数据位置、如何把数据从旧电脑搬到新电脑 | P0 |

### 8.4 已知分发问题与对策（如实写入说明文档）

| 问题 | 表现 | 对策 |
| --- | --- | --- |
| SmartScreen 拦截（未签名 exe） | 首次运行弹"Windows 已保护你的电脑" | 说明文档写清"更多信息 → 仍要运行"步骤；长期方案：购买代码签名证书（EV/OV）签名后消除（列入 P2，成本另议） |
| 杀毒软件误报 | 部分杀软对打包后的 exe 误报 | 提交主流杀软厂商白名单申诉；保持包体无混淆；说明文档写明 |
| 系统缺少 WebView2 运行时 | 便携版双击无反应 | Win10 21H2+/Win11 已内置；老旧系统按说明文档安装微软 WebView2 运行时；安装版自动引导安装 |
| 未解压直接双击 | 数据写入临时目录，重启丢失 | FR-DIS-05 检测提示 |

### 8.5 打包流水线

| 编号 | 需求描述 | 优先级 |
| --- | --- | --- |
| FR-DIS-30 | **一键打包脚本**：一条命令产出两个产物（zip + exe）+ sha256 校验文件；版本号、应用名、图标统一管理 | P0 |
| FR-DIS-31 | **产物清单**：每次发布产出 ① 便携 zip ② 安装 exe ③ 校验值 ④ 《使用说明.txt》⑤ 更新日志 | P0 |
| FR-DIS-32 | **打包自检**：CI 本地脚本自动在干净环境验证"解压即用"与"静默安装成功" | P1 |

---

## 9. 非功能需求

| 类别 | 需求 |
| --- | --- |
| 性能 | 冷启动 ≤ 3 秒；明细 1 万条滚动流畅（虚拟列表）；统计页 ≤ 2 秒；100 条导入解析 ≤ 10 秒；媒体缩略图加载 ≤ 0.5 秒/张 |
| 兼容性 | Windows 10 21H2+ / Windows 11（64 位）；高 DPI 缩放（125%/150%）界面不错位 |
| 体积 | 便携 zip ≤ 30MB、安装 exe ≤ 20MB（Tauri 2 目标值，不含用户数据） |
| 稳定性 | SQLite WAL + 崩溃自动恢复；强杀进程不丢数据；未捕获异常弹友好提示并可导出日志（`data/logs/`） |
| 安全隐私 | 默认零联网（见 6.3）；AI Key 仅存本机；账单数据不加密存储（自用场景，P2 可选数据目录加密） |
| 可维护性 | 解析规则/统计口径/AI Prompt 与小程序版共用同一套代码库模块（monorepo） |

---

## 10. 数据模型（SQLite 核心表）

与小程序版对齐，去掉账号/会员相关表（user、order、product），保留：`book`（单行）、`category`、`account`、`bill`、`media`、`cycle_rule`、`import_task`、`import_item`；新增本地专属表：

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| config | key / value | 主题、AI Key、数据目录迁移状态等（或直接用 config.json） |
| schema_migrations | version / applied_at | 数据库结构版本与迁移记录（FR-L-54） |
| ai_insight（V1.1） | type / cycle_ref / content_json / model / cost | 本地 AI 建议记录与成本统计 |

字段定义沿用小程序 PRD 第 11 章（bill.amount 分存储、media 含 thumb_key 等），不重复展开。

---

## 11. 技术方案（评审已确认：Tauri 2）

### 11.1 选型结论与理由

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 桌面外壳 | **Tauri 2（Rust）** | ① 新：当前桌面技术栈最活跃的方向；② 企业采用快速增长，Rust 本身在大厂（微软/谷歌/腾讯/字节/华为等）应用广泛；③ 个人职业：C++ 背景上手 Rust 最快，"C++/Rust + Python + TS"是简历强组合；④ 包体 5~15MB，完美契合 zip 分发；⑤ 依赖系统 WebView2，Win10 21H2+/Win11 内置 |
| 前端 UI | **Vue 3 + TypeScript + Vite** | 与小程序版共享组件/样式/解析逻辑；TS 是企业前端主流，就业池最宽 |
| 数据库 | SQLite（tauri-plugin-sql / rusqlite） | 单文件、零部署、WAL 安全 |
| 图表 | ECharts（前端渲染） | 与小程序版同一套配置 |
| HTTP/AI | Rust 侧 reqwest 代理 DeepSeek | Key 仅存本机配置、不暴露前端；统一超时/重试 |
| 打包 | NSIS/MSI 安装器 + 便携 zip 脚本 | 一键双产物 |

### 11.2 职业发展视角（本选型的核心依据）

1. **Rust 升维**：C++ 背景学习 Rust 有天然优势，Rust 是近年需求增速最快的系统语言；桌面工具/基础设施/AI 工程化岗位对"C++/Rust 双栈"认可度高；
2. **TS 补全 Web 技能**：Vue3/TS 同时补齐前端能力，就业面最宽，且与小程序版直接复用；
3. **与企业采用趋势对齐**：相比纯 Electron 路线，牺牲部分生态成熟度，换取"体积 + 职业升维"双重收益；Electron 生态最成熟但技能溢价有限。

### 11.3 与小程序版的复用清单（monorepo 建议）

- `shared/`（TS 纯逻辑，两端共用）：分类体系、周期规则引擎（纯函数）、导入解析规则与正则、AI Prompt 模板与聚合数据生成器、统计口径函数；
- 前端 UI 组件（Vue）：记一笔/明细/统计/导入等页面组件与样式主题可直接移植到小程序（Taro 支持 Vue3）。

### 11.4 应用标识规范（已确认）

| 项 | 规范 |
| --- | --- |
| 名称 | **铭记日常**（桌面版安装名为「铭记日常」） |
| 图标 | 一本**账本** + 账本上方字母 **「M」**；需产出 ICO（16~256 多尺寸）与 PNG（快捷方式/托盘/应用内） |
| 图标要求 | 账本造型简洁、M 用主色 #00B578 或深色，小尺寸（16px）下 M 仍可辨识 |

### 11.5 备选方案（止损预案）

若 Rust 上手受阻：**1 周内切换 Electron + Vue3 + TS**——前端代码 100% 保留，仅壳层更换，第 8 章分发规格不变（体积目标相应放宽到 150MB）。

---

## 12. 里程碑与开发计划（个人开发者）

> 原则：**先打通分发，再堆功能**——第一周就产出能发给朋友的"空壳安装包"，验证 zip/exe 链路，避免最后被打包问题卡住。

| 里程碑 | 内容 | 估时（人日） |
| --- | --- | --- |
| M0 脚手架与分发链路 | Tauri 2 工程（Rust 壳 + Vue3/TS 前端）、SQLite（tauri-plugin-sql）接入、双产物配置（NSIS 安装器 + 便携 zip 脚本）、图标（账本+M）与版本管理、**空壳双产物打出发给朋友验证**（含 SmartScreen 说明文档） | 5 |
| M1 基础记账 | 记一笔、明细、分类/账户、账单 CRUD | 4 |
| M2 周期能力 | 周期规则引擎（复用小程序设计）、管理页、归属重算 | 3 |
| M3 导入识别 | 文件选择、docx 解包、规则解析、AI 兜底（可选 Key）、预览确认、撤销 | 5 |
| M4 统计与媒体 | 统计四件套（ECharts）、媒体本地存储/缩略图/预览 | 5 |
| M5 数据管理与打磨 | 备份/恢复/导出/迁移、崩溃恢复、主题、日志、测试回归 | 4 |
| M6 内测发布 | 双产物 + 说明文档 + 3 位朋友实测验收 | 2 |
| **合计** | | **约 28 人日（全职约 6 周，兼职约 11~14 周）** |

---

## 13. 验收标准（整体）

1. **分发验收（硬门槛）**：在一台全新 Windows 电脑（无任何开发环境）上——便携 zip 解压即用 ✅、安装 exe 安装/卸载全流程 ✅、两种形态记一笔后重启数据均在 ✅、中文路径 ✅、非管理员账户 ✅；
2. 四类核心功能达到第 7 章各节验收标准；
3. 数据安全：强杀进程、断电模拟、卸载重装三场景数据零丢失；
4. 离线可用：断网状态下全功能（除 AI）正常；
5. 默认零联网验证：抓包确认未开启 AI 时无任何网络请求；
6. 与小程序版对账：同一份测试账单在两边统计口径结果一致。

---

## 14. 风险与开放问题

### 14.1 风险清单

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 未签名 exe 被 SmartScreen/杀软拦截 | 朋友拿到不敢装 | 说明文档写清操作步骤；后续购买代码签名证书（P2） |
| Rust 学习曲线（首次使用） | 前期开发速度偏慢 | M0 预留学习与骨架搭建时间；解析/AI 逻辑放 TS 层降低 Rust 比重；1 周内受阻切换 Electron 止损（11.5） |
| AI Key 由用户自备，朋友测试用不了 AI | 导入自由文本识别率下降 | 规则解析为主路径；AI 仅兜底；文档说明"可选配置" |
| 便携版数据写在只读/临时目录 | 数据丢失 | FR-DIS-05 检测 + 回退 APPDATA |
| 与小程序版口径漂移 | 两边数据对不上 | monorepo 共享模块 + 对账测试用例 |

### 14.2 评审决策记录（已确认）

| # | 决策项 | 结论 |
| --- | --- | --- |
| 1 | 产品名称 | **铭记日常** |
| 2 | 图标 | 账本 + 上方字母「M」（按 11.4 出图，多尺寸 ICO/PNG） |
| 3 | 技术栈 | **Tauri 2（Rust）+ Vue 3 + TypeScript + Vite**；备选 Electron（止损预案见 11.5） |
| 4 | AI 策略 | **用户自备 DeepSeek API Key**，AI 功能默认关闭、设置页可开启/关闭；导入 AI 兜底同规则 |
| 5 | 分发方式 | 便携 zip + 安装 exe 双产物（原需求不变） |

### 14.3 剩余开放问题

1. 导入 AI 兜底入口是否在 V1.0 提供（默认：提供，默认关闭）；
2. 便携版数据加密、自动更新（默认均为 P2）；
3. 分发对象范围与反馈收集方式；
4. 是否购买代码签名证书（默认后续再议）。

---

## 15. 假设清单

1. 目标系统为 Windows 10 21H2+ / 11（64 位），不考虑 Win7/macOS；
2. 本地版免费、无账号、无云同步；数据以"导出文件"方式与未来小程序版互通（V1.1+ 评估云同步）；
3. 技术栈已确认：Tauri 2（Rust）+ Vue 3 + TypeScript + Vite（见 14.2 决策记录），前端 UI 与小程序版共享组件与样式；
4. 媒体文件不设容量配额（本地磁盘为限），仅做占用提示；
5. 代码签名、自动更新、数据加密均为 P2，V1.0 用说明文档弥补体验；
6. 解析规则、分类体系、统计口径、AI Prompt 与小程序版保持一致（monorepo 共享）。

---

## 附录 A：便携版 zip 解压后目录结构

```
铭记日常-便携版/
├── 铭记日常.exe          # 主程序（单文件，前端资源已内嵌）
├── data/                 # 首次运行自动创建（分发包中不含）
│   ├── app.db
│   ├── media/
│   ├── backups/
│   ├── logs/
│   └── config.json
├── 使用说明.txt
└── 更新日志.txt
```

## 附录 B：安装版目录结构

```
%LOCALAPPDATA%\Programs\铭记日常\   # 程序目录（卸载时删除）
%APPDATA%\铭记日常\                 # 数据目录（卸载默认保留）
├── app.db / media/ / backups/ / logs/ / config.json
```

## 附录 C：发布产物清单（每次版本发布）

| 产物 | 说明 |
| --- | --- |
| `铭记日常-便携版-v1.0.0-win-x64.zip` | 解压即用 |
| `铭记日常-Setup-1.0.0.exe` | 安装向导 |
| `sha256sums.txt` | 两产物的校验值 |
| `使用说明.txt` | 含 SmartScreen 处理、数据位置、迁移方法 |
| `更新日志.txt` | 本版变更 |

---

# 16. V1.2 迭代需求（2025-XX 评审访谈结论）

> 本章由"技术负责人 × 产品负责人"三轮访谈确定，作为 v1.2.0 / v1.2.1 / v1.3.0 三个迭代的开发依据。

## 16.1 访谈结论汇总

| 决策项 | 结论 |
| --- | --- |
| 主线 | **留存做稳**；AI 功能同期落地（靠排期协调） |
| 代码仓库 | **GitHub 公开仓库**（个人品牌 + Releases 分发） |
| 自动更新 | **Tauri 官方 updater 插件 + 自签密钥**（零成本；不购买代码签名证书，SmartScreen 现状维持） |
| 更新源 | **GitHub Releases**（网盘分享链接无法做直链） |
| AI Key | **引导用户自备 DeepSeek Key**：设置页内置图文获取教程 + 粘贴即用 + 一键连接测试 |
| AI 范围 | 导入 AI 兜底 + 周期报告 + 省钱建议 + 饮食推荐（**带菜谱外链**，默认浏览器打开） |
| AI 收费 | **留会员口子**：本期全部免费但标注"内测功能"，设置页留总开关（后续可切换为会员权益） |
| 用户反馈 | **微信群接收**：反馈表单 + 日志一键复制，引导粘贴到群 |
| 崩溃上报 | **本地生成报告手动发送**：崩溃自动落盘，下次启动弹窗征询，用户复制发送；零云端依赖 |
| 工程质量 | git 初始化 + **vitest 核心引擎单测**（周期引擎、导入解析引擎） |

## 16.2 迭代版图与排期

| 版本 | 内容 | 周期 |
| --- | --- | --- |
| **v1.2.0 基建** | git 仓库 + GitHub 公开推送；vitest 引擎单测；崩溃捕获与报告；应用内反馈；打包与发布脚本适配 Releases | 1 周 |
| **v1.2.1 自动更新** | tauri-plugin-updater + 自签密钥；安装版自动更新、便携版提示引导；GitHub Releases 首次发版 | 1 周 |
| **v1.3.0 AI** | AI 配置页（Key 教程/测试/总开关）；导入 AI 兜底；统计页 AI 三功能（报告/省钱/饮食，带菜谱外链，标注内测） | 2~3 周 |

## 16.3 功能需求明细

### FR-NEW-01 工程质量（v1.2.0，P0）

- git 仓库初始化为**公开仓库**推至 GitHub（仓库含源码与需求文档；`.gitignore` 排除构建产物与数据）
- 引入 **vitest**，为以下核心引擎编写单元测试，`npm test` 一键回归：
  - 周期引擎：按月（25 日发薪日）、按周、自定义（14 天/2 个月）、跨年顺延、双向历史展开、日期归属
  - 解析引擎：各类日期格式（回归 `2025-01-15` 不产生 `2025-001-15`）、金额格式、一行多笔、标题年份推断、Markdown 跳过、自定义列规则
- 验收：`npm test` 全绿；git 历史含里程碑提交；GitHub 仓库可见

### FR-NEW-02 崩溃捕获与报告（v1.2.0，P0）

- Rust 侧安装 **panic hook**：崩溃时写入 `data/logs/crash-<时间戳>.log` 并标记 `crash.pending`（release 的 panic=abort 下 hook 仍执行）
- 下次启动检测到标记 → 弹窗："检测到上次异常退出，是否复制崩溃报告发给开发者？"提供【复制】【忽略】
- 复制内容含：版本号、系统信息、崩溃日志；复制后提示粘贴到反馈群
- 验收：人为触发 panic（调试命令或断言语义）→ 重启出现弹窗 → 复制内容含崩溃信息 → 忽略后标记清除、不再弹

### FR-NEW-03 应用内反馈（v1.2.0，P0）

- 设置 → 通用 → 「意见反馈」：问题描述（必填）+ 联系方式（选填）→ 生成反馈文本（自动附版本号与最近日志摘要）→ 一键复制
- 提示用户粘贴到微信群；零后端依赖
- 验收：生成文本包含描述/版本/日志；复制按钮在无剪贴板权限时降级为"全选后 Ctrl+C"提示

### FR-NEW-04 自动更新（v1.2.1，P0）

- `tauri-plugin-updater` + **自签密钥对**（`tauri signer generate`，私钥本地保存、公钥随应用内置）
- 更新源：GitHub Releases；`npm run package` 后增加 `npm run publish`（创建 Releases + 上传产物 + 生成更新清单）
- 安装版：启动时检查 → 弹窗提示 → 一键下载安装；便携版：检测到新版本 → 提示前往下载页
- 验收：发布 v1.2.2 假更新包，安装版自动升级成功且数据保留；便携版出现引导提示

### FR-NEW-05 AI 配置页（v1.3.0，P0）

- 设置 → 「AI 智能服务」：总开关（**默认关闭**，标注"内测功能"）；DeepSeek Key 输入（仅存本机）；「如何获取 Key」图文教程（注册 → 创建 Key → 粘贴）；「测试连接」按钮（调轻量接口验证 Key 有效性）
- 调用层：Rust 侧 reqwest 代理 DeepSeek API（Key 不出前端）
- 验收：无 Key 时 AI 入口提示配置；错误 Key 测试连接给出明确失败原因；开启关闭即时生效

### FR-NEW-06 导入 AI 兜底（v1.3.0，P0）

- AI 开启时，导入预览页提供「AI 智能识别失败行」按钮：将标红行的原文批量发 DeepSeek 解析（复用《AI功能设计-Prompt与接口.md》聚合规范，不传无关信息）
- 未开启/调用失败 → 降级纯规则解析，功能不中断；结果与规则解析同格式进预览表
- 验收：自由文本样本识别准确率 ≥ 85%；关闭 AI 时按钮隐藏/置灰且纯规则可用

### FR-NEW-07 AI 智能建议（v1.3.0，P0）

- 统计页「AI 分析」入口：周期报告 / 省钱建议 / 饮食推荐（复用已设计 Prompt：建议必须带数据依据+量化预期，禁止投资/医疗建议）
- 饮食推荐**带菜谱外链**（每条约 1~3 个外链，点击用默认浏览器打开；页面注明"外部链接仅供参考"）
- 结果卡片式展示 + 有用/无用反馈；固定免责声明；所有 AI 功能标注"内测"
- 验收：P95 生成 ≤ 15 秒；建议 100% 含数据依据；外链可打开；关闭授权/开关后接口拒绝调用

## 16.4 后续候选（未列入本期）

预算与提醒、多账本、年度报告、小程序版、代码签名证书、会员体系（AI 付费墙切换开关已预留）

